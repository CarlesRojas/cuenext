<?xml version="1.0" encoding="utf-8"?>
<!-- One poster cell of the grid, the widget's PosterCard. The poster bitmap (pre-rounded
     by WidgetApi, always 2:3 - a placeholder when there is no image) drives the cell
     height via adjustViewBounds, so cells keep the card aspect at any column width. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/item_poster"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:contentDescription="@string/widgetPosterDescription"
        android:scaleType="fitXY" />

    <TextView
        android:id="@+id/item_fallback"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="4"
        android:padding="8dp"
        android:textColor="#FF737373"
        android:textSize="12sp"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/item_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginStart="10dp"
        android:max="100"
        android:progressDrawable="@drawable/widget_progress"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/item_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="4dp"
        android:background="@drawable/widget_button_background"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingBottom="5dp"
        android:paddingEnd="7dp"
        android:paddingStart="7dp"
        android:paddingTop="5dp">

        <TextView
            android:id="@+id/item_button_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:textColor="#FFFFFFFF"
            android:textSize="11sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/item_button_icon"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:contentDescription="@string/widgetButtonDescription" />
    </LinearLayout>
</FrameLayout>
