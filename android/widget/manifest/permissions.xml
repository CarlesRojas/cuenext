    <!-- cuenext-widget:permissions:begin -->
    <!-- The widget fetches watchlist data and posters itself; the TWA alone doesn't need
         this because Chrome does its networking. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- cuenext-widget:permissions:end -->
